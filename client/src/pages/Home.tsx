/** College Press: the public page reads as a service folio, from masthead through direct order action. */
import { FaqSection } from "@/components/FaqSection";
import { ServiceCatalogue } from "@/components/ServiceCatalogue";
import { SiteHeader } from "@/components/SiteHeader";
import { isConfiguredTelegramUsername, isConfiguredWhatsAppNumber, TELEGRAM_USERNAME, WHATSAPP_NUMBER } from "@/config/contact";
import { useCart } from "@/contexts/CartContext";
import { calculateSubtotal, formatRupees } from "@/lib/pricing";
import { ArrowDownRight, ArrowUpRight, ShoppingBag } from "lucide-react";
import { lazy, Suspense, useState } from "react";
const CartDialog = lazy(() => import("@/components/CartDialog").then((module) => ({ default: module.CartDialog })));
const OrderDetailsDialog = lazy(() => import("@/components/OrderDetailsDialog").then((module) => ({ default: module.OrderDetailsDialog })));
export default function Home() { const { items, itemCount } = useCart(); const [cartOpen, setCartOpen] = useState(false); const [detailsOpen, setDetailsOpen] = useState(false); const [cartMessage, setCartMessage] = useState(""); const subtotal = calculateSubtotal(items); function contactUnavailable(channel: "WhatsApp" | "Telegram"): void { const configured = channel === "WhatsApp" ? isConfiguredWhatsAppNumber(WHATSAPP_NUMBER) : isConfiguredTelegramUsername(TELEGRAM_USERNAME); if (!configured) setCartMessage(`${channel} ordering needs configuration before it can open.`); } return <div className="site-shell" id="top"><SiteHeader itemCount={itemCount} onOpenCart={() => setCartOpen(true)} /><main><section className="hero-section" aria-labelledby="hero-title"><div className="hero-folio-mark" aria-hidden="true">SB<br /><span>26</span></div><div className="hero-content"><p className="eyebrow">STUDENT SERVICES · MCC</p><h1 id="hero-title">Present your work<br /><em>with confidence.</em></h1><p className="hero-copy">Presentations, project reports, document formatting and student essentials — clearly priced and delivered as editable files.</p><div className="hero-actions"><a className="primary-action" href="#services">Browse services <ArrowDownRight size={17} aria-hidden="true" /></a><a className="text-action" href="#how-it-works">How ordering works <span aria-hidden="true">→</span></a></div><p className="hero-microcopy">Same pricing across departments <span aria-hidden="true">·</span> Editable files included</p></div></section><section className="trust-strip" aria-label="SoftBazzar principles"><div><strong>CLEAR PRICING</strong><span>Know the rate before you enquire.</span></div><div><strong>EDITABLE FILES</strong><span>Stay in control of final changes.</span></div><div><strong>DIRECT ORDERING</strong><span>Send the brief when you are ready.</span></div></section><ServiceCatalogue onAdded={setCartMessage} /><section className="process-section" id="how-it-works" aria-labelledby="process-title"><div className="process-heading"><p className="eyebrow">HOW IT WORKS</p><h2 id="process-title">A short route from brief to order.</h2></div><ol className="process-list"><li><span>01</span><h3>Choose your service</h3><p>Select the format and size you need.</p></li><li><span>02</span><h3>Review your order</h3><p>Add your deadline and requirements.</p></li><li><span>03</span><h3>Send to SoftBazzar</h3><p>Continue through WhatsApp or Telegram and confirm availability.</p></li></ol></section><FaqSection /><section className="final-cta"><p className="eyebrow">READY WHEN YOU ARE</p><h2>Have something<br /><em>due soon?</em></h2><p>Select what you need, review the price, and send us the details.</p><a href="#services" className="primary-action">Browse services <ArrowUpRight size={17} aria-hidden="true" /></a></section></main><footer className="site-footer"><div className="footer-top"><div><a href="#top" className="wordmark"><span className="monogram" aria-hidden="true">SB</span><span>SOFT<span className="wordmark-rule" />BAZZAR</span></a><p>Student presentation and document support.</p></div><nav aria-label="Footer navigation"><a href="#services">Services</a><a href="#how-it-works">How it works</a><a href="#faq">FAQ</a><button type="button" onClick={() => contactUnavailable("WhatsApp")}>WhatsApp</button><button type="button" onClick={() => contactUnavailable("Telegram")}>Telegram</button></nav></div><div className="footer-bottom"><span>© 2026 SoftBazzar. All rights reserved.</span><span>Independent student-support service. Not affiliated with Madras Christian College.</span></div></footer><p className="sr-only" aria-live="polite">{cartMessage}</p>{itemCount > 0 && <button type="button" className="mobile-cart-bar" onClick={() => setCartOpen(true)}><span><ShoppingBag size={17} aria-hidden="true" /> {itemCount} {itemCount === 1 ? "item" : "items"}</span><strong>{formatRupees(subtotal)}</strong><b>View order <span aria-hidden="true">→</span></b></button>}<Suspense fallback={null}><CartDialog open={cartOpen} onOpenChange={setCartOpen} onReview={() => { setCartOpen(false); setDetailsOpen(true); }} /><OrderDetailsDialog open={detailsOpen} onOpenChange={setDetailsOpen} /></Suspense></div>; }
