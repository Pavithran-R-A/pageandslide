/** College Press: a restrained masthead with high-clarity navigation and cart access. */
import { ShoppingBag } from "lucide-react";
type SiteHeaderProps = Readonly<{ itemCount: number; onOpenCart: () => void }>;
export function SiteHeader({ itemCount, onOpenCart }: SiteHeaderProps) { return <header className="site-header"><div className="topbar-inner"><a href="#top" className="wordmark" aria-label="SoftBazzar home"><span className="monogram" aria-hidden="true">SB</span><span>SOFT<span className="wordmark-rule" />BAZZAR</span></a><nav className="site-nav" aria-label="Primary navigation"><a href="#services">Services</a><a href="#how-it-works">How it works</a><a href="#faq">FAQ</a></nav><button className="cart-toggle" type="button" onClick={onOpenCart} aria-label={`Open cart, ${itemCount} items`}><ShoppingBag size={18} strokeWidth={1.7} aria-hidden="true" /><span>Cart</span>{itemCount > 0 && <span className="cart-count" aria-hidden="true">{itemCount}</span>}</button></div></header>; }
